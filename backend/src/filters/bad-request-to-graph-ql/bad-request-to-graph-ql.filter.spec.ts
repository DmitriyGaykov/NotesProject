import { BadRequestToGraphQlFilter } from './bad-request-to-graph-ql.filter';

describe('BadRequestToGraphQlFilter', () => {
  it('should be defined', () => {
    expect(new BadRequestToGraphQlFilter()).toBeDefined();
  });
});
